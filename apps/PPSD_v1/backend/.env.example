FDSNWS_URL=http://172.31.100.100
LOG_LEVEL=INFO
MAX_WORKERS=4
MAX_TARGETS=20

# PPSD result store root (SeisComP SDS structure, daily npz)
# PPSD_SDS_DIR=./sds

# Optional default PPSD plot axis limits (X: period[s] or frequency[Hz] per request)
# PPSD_X_MIN=0.1
# PPSD_X_MAX=100
# PPSD_Y_MIN=-200
# PPSD_Y_MAX=-50
# PPSD_YAXIS_TYPE=acceleration
#   acceleration | velocity | velocity_nm | displacement | pressure
# PPSD_PRESSURE_DB_OFFSET=0
