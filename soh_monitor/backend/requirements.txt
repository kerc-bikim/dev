# soh_monitor 백엔드 의존성.
# api / collector / edge 세 프로세스가 같은 코드베이스를 공유하므로 한 파일로 고정한다.

# --- 웹/API ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# --- 기록계 통신 ---
httpx==0.28.1

# --- 설정 DB (PostgreSQL) ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- 시계열 DB ---
influxdb-client==1.48.0

# --- 계약 검증 ---
PyYAML==6.0.2
jsonschema==4.23.0

# --- 테스트 ---
pytest==8.3.4
pytest-asyncio==0.24.0
respx==0.22.0
